# Ansible Galaxy collections - deploy playbook ke liye zaroori
# Install:  ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.docker
    version: ">=3.4.0"
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
